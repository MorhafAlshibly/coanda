{
	"name": "itemsarchiver",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"clean": "rimraf dist",
		"prestart": "npm run clean && npm run build",
		"start": "func start",
		"test": "jest"
	},
	"dependencies": {
		"@azure/functions": "^4.0.0-alpha.7"
	},
	"devDependencies": {
		"@types/node": "^18.x",
		"jest": "^29.6.2",
		"rimraf": "^5.0.0",
		"ts-jest": "^29.1.1",
		"typescript": "^4.0.0"
	},
	"main": "dist/src/functions/*.js"
}
